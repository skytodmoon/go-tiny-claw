# Go Tiny Claw 项目分析

## 核心功能

1. **慢思考机制（Thinking Phase）**
   - Agent 在执行任务前先进行结构化思考，输出详细计划。
   - 区分 Thinking 和 Acting 阶段，确保决策的科学性。

2. **工具调用（Tool Registry）**
   - 支持多种工具（如文件读取、写入、编辑，Bash 命令执行等）。
   - 通过统一注册与分发机制动态选择工具。

3. **多步任务执行**
   - Agent 能够完成复杂的多步任务，并根据执行结果动态调整行为。

4. **四阶段循环（Thinking → Acting → Observation → Re-thinking）**
   - 在 Level 3 中实现了一个完整的反馈循环，Agent 可以根据观察结果重新思考并调整策略。

5. **上下文管理**
   - 动态上下文组装和 Token 预算管理，确保高效利用 LLM 资源。
   - 提供自动压缩机制以处理长上下文。

6. **多 Provider 支持**
   - 可无缝对接 OpenAI、Claude、MiniMax、DeepSeek 等多种 LLM Provider。

7. **日志系统**
   - 模块化日志记录，支持多种日志级别和格式化输出。
   - 日志文件按日期分割，方便调试与问题追踪。

## 架构特点

1. **模块化设计**
   - 核心功能（如 MainLoop、Providers、Tools）按模块划分，便于扩展和维护。
   - 各组件通过接口定义，支持插件化扩展。

2. **任务导向架构**
   - 提供 15 种测试任务套件，覆盖从基础到复杂的各种场景。
   - 支持单次任务和多步任务的执行。

3. **动态上下文工程**
   - 根据任务需求动态组装和优化上下文，提高资源利用率。

4. **工具注册表机制**
   - 工具以插件形式注册，Agent 可以动态选择最合适的工具完成任务。

5. **测试驱动开发**
   - 内置丰富的测试任务和验证机制，确保代码质量。

## 项目结构

```
go-tiny-claw/
├── cmd/                  # 程序入口和测试套件
├── internal/             # 核心实现模块
│   ├── engine/           # MainLoop 实现
│   ├── provider/         # LLM Provider 适配器
│   ├── context/          # 上下文管理
│   ├── tools/            # 工具注册表与内置工具
│   ├── memory/           # 文件的记忆存储
│   ├── feishu/           # 飞书机器人集成
│   └── logger/           # 日志系统
├── integration/          # 集成测试
└── testutils/            # 测试工具
```

## 结论

Go Tiny Claw 是一个功能强大且灵活的 AI Agent 框架，尤其适合需要多步任务执行和动态工具调用的场景。其模块化设计和丰富的测试套件确保了高可用性和易扩展性。