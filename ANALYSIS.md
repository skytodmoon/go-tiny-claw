# Go Tiny Claw 项目分析

## 核心功能

1. **AI Agent 框架**: 一个轻量级的 AI 智能助手框架，使用 Go 语言实现，支持动态工具调用和多步任务执行。
2. **核心理念**: 强调"能慢思考、会用工具、能推进任务"的 Agent 设计。
3. **多 Provider 支持**: 整合了多个 LLM Provider（如 OpenAI、Claude、MiniMax、DeepSeek、GLM47、SiliconFlow）。
4. **工具注册与调用**: 提供文件读写、编辑和 Bash 命令等功能，支持动态工具选择和注册机制。
5. **任务循环**: 实现四阶段循环（Thinking → Acting → Observation → Re-thinking），确保任务高效推进。
6. **上下文管理**: 动态组装上下文，支持 Token 监控和自动压缩，优化资源使用。
7. **日志系统**: 提供模块化日志和彩色输出，支持按日期分割日志文件。

## 架构特点

1. **分层设计**:
   - **核心引擎**: `MainLoop` 实现任务循环机制。
   - **Provider 适配层**: 支持多 LLM Provider，提供统一的接口定义。
   - **工具层**: 工具注册表管理，允许动态扩展工具功能。
   - **上下文层**: 上下文动态组装和压缩机制，优化交互效率。

2. **模块化日志系统**:
   - 每个模块独立日志记录，支持不同日志级别（DEBUG、INFO、WARN、ERROR、FATAL）。
   - 日志格式化输出（时间戳、级别、模块名）。

3. **测试驱动**:
   - 提供 15 种测试任务，涵盖文件读写、内容分析、代码生成、错误处理等场景。
   - 支持多任务运行验证框架稳定性。

4. **轻量级与扩展性**:
   - 代码结构清晰（`internal` 目录下按功能分模块）。
   - 支持自定义工具和 Provider 扩展。

5. **开发友好**:
   - 提供快速启动脚本和详细的文档说明。
   - 集成飞书机器人等实际应用场景。

## 版本规划

1. **Level 1**: 基础功能（慢思考机制、工具调用、单次任务执行）。
2. **Level 2**: 进阶功能（结构化思考、多步执行、动态工具选择）。
3. **Level 3**: 对标 Claude Code 最佳实践（四阶段循环、上下文工程、自动压缩机制）。

## 总结

Go Tiny Claw 是一个功能丰富、架构清晰的 AI Agent 框架，适用于需要动态工具调用和任务推进的场景。其分层设计和模块化日志系统提升了开发效率，而多 Provider 支持和测试套件则保证了框架的稳定性和扩展性。