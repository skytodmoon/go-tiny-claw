# Code Review: `internal/engine/loop.go`

## 代码结构分析

1. **核心结构体**
   - `AgentEngine`: 主引擎，负责任务调度和工具调用。
   - `QueryState`: 记录循环状态，包括轮次、阶段、思考结果等。
   - `ContextLayer`: 管理上下文（系统提示、对话历史等）。

2. **主要方法**
   - `buildSystemPrompt`: 根据阶段动态生成系统提示。
   - `estimateTokens` 和 `shouldCompact`: 计算和检查上下文的 token 使用情况。
   - `compactContext`: 压缩上下文以节省资源。
   - `Run`: 执行主循环，处理用户输入和工具调用。

## 优化点

### 1. 上下文压缩逻辑
- **问题**: 当前压缩简单粗暴，可能导致关键信息丢失。
- **建议**: 引入智能摘要或基于关键字的保留策略。

### 2. 日志输出
- **问题**: 日志内容繁多，难以快速定位问题。
- **建议**: 增加日志级别动态调整功能或减少非关键日志。

### 3. 错误处理
- **问题**: 工具调用失败后仅有简单错误返回。
- **建议**: 增加重试机制或提供更详细的错误上下文。

### 4. 性能优化
- **问题**: `estimateTokens` 方法过于简单，可能不准确。
- **建议**: 使用更精确的 token 计算方式或缓存计算结果。

## 改进建议

- **上下文管理**: 引入 NLP 技术优化摘要生成。
- **日志系统**: 支持动态日志级别调整。
- **工具调用**: 增加并发支持以提升效率。
- **测试覆盖**: 补充单元测试，特别是压缩逻辑和工具调用部分。

## 后续任务
1. 实现智能上下文压缩。
2. 优化日志输出机制。
3. 增强错误处理和重试逻辑。
4. 补充测试用例。