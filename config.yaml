# Database Configuration
database:
  type: mysql
  host: localhost
  port: 3306
  username: root
  password: ""
  name: mydb

# Logging Configuration
logging:
  level: info
  file: ./logs/app.log
  console: true

# Application Settings
app:
  name: myapp
  version: 1.0.0
  port: 8080
  env: development